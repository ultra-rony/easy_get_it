name: easy_get_it
description: A simple and lightweight service locator for managing dependencies in Flutter and Dart applications.
version: 0.0.4
repository: https://github.com/ultra-rony/easy_get_it
homepage: https://github.com/ultra-rony/easy_get_it

topics: [ dependency-injection, service-locator, inversion-of-control, di-container, di ]

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

screenshots:
  - description: The logo of the easy_get_it package.
    path: screenshots/logo.png